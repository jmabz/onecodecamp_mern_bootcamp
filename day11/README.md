# Lessons Learned

## React Bootstrap
Library that provides ready-to-use React components integrated with Bootstrap.

## React Router
Library built on top of React that lets one render different views in order to help produce a multiple-page React app.

### Folder structure
As a basic approach to organize a multiple-page React app, within the `src` folder, components tied to separate pages for each route can be placed in a `pages` folder.